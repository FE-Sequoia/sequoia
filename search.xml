<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>CSS3-Grid布局使用教程</title>
    <url>/sequoia/posts/28aad683.html</url>
    <content><![CDATA[<h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><hr>
<p>CSS3的 网格布局（Grid）是最强大的 CSS 布局方案。</p>
<p>它将网页划分成一个个网格，可以任意组合不同的网格，做出各种各样的布局。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109112509.png" alt="2020240109112509.png"> </p>
<p>上图这样的布局，就可以用 Grid 布局轻松实现。</p>
<p>Grid 布局和 Flex 布局有一定的相似性，都可以指定容器内部多个项目的位置。但是，它们也存在重大区别。</p>
<p>Flex 布局是轴线布局，只能指定”项目”针对轴线的位置，可以看作是<strong>一维布局</strong>。</p>
<p>Grid 布局则是将容器划分成”行”和”列”，产生单元格，然后指定”项目所在”的单元格，可以看作是<strong>二维布局</strong>。</p>
<p>Grid 布局远比 Flex 布局强大。</p>
<h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><hr>
<p>学习 Grid 布局之前，需要了解一些基本概念。</p>
<h3 id="2-1-容器和项目"><a href="#2-1-容器和项目" class="headerlink" title="2.1 容器和项目"></a>2.1 容器和项目</h3><p>采用网格布局的区域，称为”容器”（container）。容器内部采用网格定位的子元素，称为”项目”（item）。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span>&gt;</span><span class="tag">&lt;<span class="name">p</span>&gt;</span>1<span class="tag">&lt;/<span class="name">p</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span>&gt;</span><span class="tag">&lt;<span class="name">p</span>&gt;</span>2<span class="tag">&lt;/<span class="name">p</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span>&gt;</span><span class="tag">&lt;<span class="name">p</span>&gt;</span>3<span class="tag">&lt;/<span class="name">p</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，最外层的<code>&lt;div&gt;</code>元素就是容器，内层的三个<code>&lt;div&gt;</code>元素就是项目。</p>
<blockquote>
<p>注意：项目只能是容器的顶层子元素，不包含项目的子元素，比如上面代码的<code>&lt;p&gt;</code>元素就不是项目。Grid 布局只对项目生效。</p>
</blockquote>
<h3 id="2-2-行和列"><a href="#2-2-行和列" class="headerlink" title="2.2 行和列"></a>2.2 行和列</h3><p>容器里面的水平区域称为”行”（row），垂直区域称为”列”（column）。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109113401.png" alt="2020240109113401.png"> </p>
<p>上图中，水平的深色区域就是”行”，垂直的深色区域就是”列”。</p>
<h3 id="2-3-单元格"><a href="#2-3-单元格" class="headerlink" title="2.3 单元格"></a>2.3 单元格</h3><p>行和列的交叉区域，称为”单元格”（cell）。</p>
<p>正常情况下，<code>n</code>行和<code>m</code>列会产生<code>n x m</code>个单元格。比如，3行3列会产生9个单元格。</p>
<h3 id="2-4-网格线"><a href="#2-4-网格线" class="headerlink" title="2.4 网格线"></a>2.4 网格线</h3><p>划分网格的线，称为”网格线”（grid line）。水平网格线划分出行，垂直网格线划分出列。</p>
<p>正常情况下，<code>n</code>行有<code>n + 1</code>根水平网格线，<code>m</code>列有<code>m + 1</code>根垂直网格线，比如三行就有四根水平网格线。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109113510.png" alt="2020240109113510.png"> </p>
<p>上图是一个 4 x 4 的网格，共有5根水平网格线和5根垂直网格线。</p>
<h2 id="容器属性"><a href="#容器属性" class="headerlink" title="容器属性"></a>容器属性</h2><hr>
<p>Grid 布局的属性分成两类。一类定义在容器上面，称为容器属性；另一类定义在项目上面，称为项目属性。这部分先介绍容器属性。</p>
<h3 id="3-1-display-属性"><a href="#3-1-display-属性" class="headerlink" title="3.1 display 属性"></a>3.1 display 属性</h3><p><code>display: grid;</code> 指定一个容器采用网格布局。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">div</span> &#123;</span><br><span class="line">  <span class="attribute">display</span>: grid;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>默认情况下，容器元素都是块级元素，但也可以设成行内元素。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">div</span> &#123;</span><br><span class="line">  <span class="attribute">display</span>: inline-grid;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码指定<code>div</code>是一个行内元素，该元素内部采用网格布局。</p>
<blockquote>
<p>注意，设为网格布局以后，容器子元素（项目）的<code>float</code>、<code>display: inline-block</code>、<code>display: table-cell</code>、<code>vertical-align</code>和<code>column-*</code>等设置都将失效</p>
</blockquote>
<h3 id="3-2-grid-template-columns、grid-template-rows-属性"><a href="#3-2-grid-template-columns、grid-template-rows-属性" class="headerlink" title="3.2 grid-template-columns、grid-template-rows 属性"></a>3.2 grid-template-columns、grid-template-rows 属性</h3><p>容器指定了网格布局以后，接着就要划分行和列。<code>grid-template-columns</code>属性定义每一列的列宽，<code>grid-template-rows</code>属性定义每一行的行高。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  <span class="attribute">display</span>: grid;</span><br><span class="line">  <span class="attribute">grid-template-columns</span>: <span class="number">100px</span> <span class="number">100px</span> <span class="number">100px</span>;</span><br><span class="line">  <span class="attribute">grid-template-rows</span>: <span class="number">100px</span> <span class="number">100px</span> <span class="number">100px</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码，指定了一个三行三列的网格，列宽和行高都是<code>100px</code>。</p>
<p>除了使用绝对单位，也可以使用百分比。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  <span class="attribute">display</span>: grid;</span><br><span class="line">  <span class="attribute">grid-template-columns</span>: <span class="number">33.33%</span> <span class="number">33.33%</span> <span class="number">33.33%</span>;</span><br><span class="line">  <span class="attribute">grid-template-rows</span>: <span class="number">33.33%</span> <span class="number">33.33%</span> <span class="number">33.33%</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>1. repeat()</strong><br>有时候，重复写同样的值非常麻烦，尤其网格很多时。这时，可以使用<code>repeat()</code>函数，简化重复的值。上面的代码用<code>repeat()</code>改写如下。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  <span class="attribute">display</span>: grid;</span><br><span class="line">  <span class="attribute">grid-template-columns</span>: <span class="built_in">repeat</span>(<span class="number">3</span>, <span class="number">33.33%</span>);</span><br><span class="line">  <span class="attribute">grid-template-rows</span>: <span class="built_in">repeat</span>(<span class="number">3</span>, <span class="number">33.33%</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>repeat()</code>接受两个参数，第一个参数是重复的次数（上例是3），第二个参数是所要重复的值。</p>
<p><code>repeat()</code>重复某种模式也是可以的。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="attribute">grid-template-columns</span>: <span class="built_in">repeat</span>(<span class="number">2</span>, <span class="number">100px</span> <span class="number">20px</span> <span class="number">80px</span>);</span><br></pre></td></tr></table></figure>

<p>上面代码定义了6列，第一列和第四列的宽度为<code>100px</code>，第二列和第五列为<code>20px</code>，第三列和第六列为<code>80px</code>。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109114141.png" alt="2020240109114141.png"> </p>
<p><strong>2. auto-fill 关键字</strong><br>有时，单元格的大小是固定的，但是容器的大小不确定。如果希望每一行（或每一列）容纳尽可能多的单元格，这时可以使用<code>auto-fill</code>关键字表示自动填充。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  <span class="attribute">display</span>: grid;</span><br><span class="line">  <span class="attribute">grid-template-columns</span>: <span class="built_in">repeat</span>(auto-fill, <span class="number">100px</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码，表示每列宽度<code>100px</code>，然后自动填充，直到容器不能放置更多的列。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109114244.png" alt="2020240109114244.png"> </p>
<p>除了<code>auto-fill</code>，还有一个关键字<code>auto-fit</code>，两者的行为基本是相同的。只有当容器足够宽，可以在一行容纳所有单元格，并且单元格宽度不固定的时候，才会有行为差异，<code>auto-fill</code>会用空格子填满剩余宽度，<code>auto-fit</code>则会尽量扩大单元格的宽度。</p>
<p><strong>3. fr 关键字</strong><br>为了方便表示比例关系，网格布局提供了<code>fr</code>关键字（fraction 的缩写，意为”片段”）。如果两列的宽度分别为<code>1fr</code>和<code>2fr</code>，就表示后者是前者的两倍。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  <span class="attribute">display</span>: grid;</span><br><span class="line">  <span class="attribute">grid-template-columns</span>: <span class="number">1</span>fr <span class="number">1</span>fr;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码表示两个相同宽度的列。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109114402.png" alt="2020240109114402.png"> </p>
<p><code>fr</code>可以与绝对长度的单位结合使用，这时会非常方便。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  <span class="attribute">display</span>: grid;</span><br><span class="line">  <span class="attribute">grid-template-columns</span>: <span class="number">150px</span> <span class="number">1</span>fr <span class="number">2</span>fr;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码表示，第一列的宽度为150像素，第二列的宽度是第三列的一半。</p>
<p><strong>4. minmax()</strong><br><code>minmax()</code>函数产生一个长度范围，表示长度就在这个范围之中。它接受两个参数，分别为最小值和最大值。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="attribute">grid-template-columns</span>: <span class="number">1</span>fr <span class="number">1</span>fr <span class="built_in">minmax</span>(<span class="number">100px</span>, <span class="number">1</span>fr);</span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>minmax(100px, 1fr)</code>表示列宽不小于<code>100px</code>，不大于<code>1fr</code>。</p>
<p><strong>5. auto 关键字</strong><br><code>auto</code>关键字表示由浏览器自己决定长度。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="attribute">grid-template-columns</span>: <span class="number">100px</span> auto <span class="number">100px</span>;</span><br></pre></td></tr></table></figure>

<p>上面代码中，第二列的宽度，基本上等于该列单元格的最大宽度，除非单元格内容设置了<code>min-width</code>，且这个值大于最大宽度。</p>
<p><strong>6. 网格线的名称</strong><br><code>grid-template-columns</code>属性和<code>grid-template-rows</code>属性里面，还可以使用方括号，指定每一根网格线的名字，方便以后的引用。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">	<span class="attribute">display</span>: grid;</span><br><span class="line">	<span class="attribute">grid-template-columns</span>: [c1] <span class="number">100px</span> [c2] <span class="number">100px</span> [c3] auto [c4];</span><br><span class="line">	<span class="attribute">grid-template-rows</span>: [r1] <span class="number">100px</span> [r2] <span class="number">100px</span> [r3] auto [r4];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码指定网格布局为3行 x 3列，因此有4根垂直网格线和4根水平网格线。方括号里面依次是这八根线的名字。</p>
<p>网格布局允许同一根线有多个名字，比如<code>[fifth-line row-5]</code>。</p>
<p><strong>7. 布局实例</strong><br><code>grid-template-columns</code>属性对于网页布局非常有用。两栏式布局只需要一行代码。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.wrapper</span> &#123;</span><br><span class="line">	<span class="attribute">display</span>: grid;</span><br><span class="line">	<span class="attribute">grid-template-columns</span>: <span class="number">70%</span> <span class="number">30%</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码将左边栏设为70%，右边栏设为30%。</p>
<p>传统的十二网格布局，写起来也很容易。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="attribute">grid-template-columns</span>: <span class="built_in">repeat</span>(<span class="number">12</span>, <span class="number">1</span>fr);</span><br></pre></td></tr></table></figure>

<h3 id="3-3-grid-row-gap-属性、grid-column-gap-属性、grid-gap-属性"><a href="#3-3-grid-row-gap-属性、grid-column-gap-属性、grid-gap-属性" class="headerlink" title="3.3 grid-row-gap 属性、grid-column-gap 属性、grid-gap 属性"></a>3.3 grid-row-gap 属性、grid-column-gap 属性、grid-gap 属性</h3><p><code>grid-row-gap</code>属性设置行与行的间隔（行间距），<code>grid-column-gap</code>属性设置列与列的间隔（列间距）。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  <span class="attribute">grid-row</span>-<span class="attribute">gap</span>: <span class="number">20px</span>;</span><br><span class="line">  <span class="attribute">grid-column</span>-<span class="attribute">gap</span>: <span class="number">20px</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>grid-row-gap</code>用于设置行间距，<code>grid-column-gap</code>用于设置列间距。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109114913.png" alt="2020240109114913.png"> </p>
<p><code>grid-gap</code>属性是<code>grid-column-gap</code>和<code>grid-row-gap</code>的合并简写形式，语法如下。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="attribute">grid-gap</span>: &lt;grid-row-gap&gt; &lt;grid-column-gap&gt;;</span><br></pre></td></tr></table></figure>

<p>因此，上面一段 CSS 代码等同于下面的代码。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  <span class="attribute">grid-gap</span>: <span class="number">20px</span> <span class="number">20px</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果<code>grid-gap</code>省略了第二个值，浏览器认为第二个值等于第一个值。</p>
<blockquote>
<p>根据最新标准，上面三个属性名的<code>grid-</code>前缀已经删除，<code>grid-column-gap</code>和<code>grid-row-gap</code>写成<code>column-gap</code>和<code>row-gap</code>，<code>grid-gap</code>写成<code>gap</code>。</p>
</blockquote>
<h3 id="3-4-grid-template-areas-属性"><a href="#3-4-grid-template-areas-属性" class="headerlink" title="3.4 grid-template-areas 属性"></a>3.4 grid-template-areas 属性</h3><p>网格布局允许指定”区域”（area），一个区域由单个或多个单元格组成。<code>grid-template-areas</code>属性用于定义区域。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  <span class="attribute">display</span>: grid;</span><br><span class="line">  <span class="attribute">grid-template-columns</span>: <span class="number">100px</span> <span class="number">100px</span> <span class="number">100px</span>;</span><br><span class="line">  <span class="attribute">grid-template-rows</span>: <span class="number">100px</span> <span class="number">100px</span> <span class="number">100px</span>;</span><br><span class="line">  <span class="attribute">grid-template-areas</span>: <span class="string">&#x27;a b c&#x27;</span></span><br><span class="line">                       <span class="string">&#x27;d e f&#x27;</span></span><br><span class="line">                       <span class="string">&#x27;g h i&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码先划分出9个单元格，然后将其定名为<code>a</code>到<code>i</code>的九个区域，分别对应这九个单元格。</p>
<p>多个单元格合并成一个区域的写法如下。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="attribute">grid-template-areas</span>: <span class="string">&#x27;a a a&#x27;</span></span><br><span class="line">                     <span class="string">&#x27;b b b&#x27;</span></span><br><span class="line">                     <span class="string">&#x27;c c c&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>上面代码将9个单元格分成<code>a</code>、<code>b</code>、<code>c</code>三个区域。</p>
<p>下面是一个布局实例。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="attribute">grid-template-areas</span>: <span class="string">&quot;header header header&quot;</span></span><br><span class="line">                     <span class="string">&quot;main main sidebar&quot;</span></span><br><span class="line">                     <span class="string">&quot;footer footer footer&quot;</span>;</span><br></pre></td></tr></table></figure>

<p>上面代码中，顶部是页眉区域<code>header</code>，底部是页脚区域<code>footer</code>，中间部分则为<code>main</code>和<code>sidebar</code>。</p>
<p>如果某些区域不需要利用，则使用”点”（<code>.</code>）表示。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="attribute">grid-template-areas</span>: <span class="string">&#x27;a . c&#x27;</span></span><br><span class="line">                     <span class="string">&#x27;d . f&#x27;</span></span><br><span class="line">                     <span class="string">&#x27;g . i&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>上面代码中，中间一列为点，表示没有用到该单元格，或者该单元格不属于任何区域。</p>
<blockquote>
<p>注意，区域的命名会影响到网格线。每个区域的起始网格线，会自动命名为<code>区域名-start</code>，终止网格线自动命名为<code>区域名-end</code>。<br>比如，区域名为<code>header</code>，则起始位置的水平网格线和垂直网格线叫做<code>header-start</code>，终止位置的水平网格线和垂直网格线叫做<code>header-end</code>。</p>
</blockquote>
<h3 id="3-5-grid-auto-flow-属性"><a href="#3-5-grid-auto-flow-属性" class="headerlink" title="3.5 grid-auto-flow 属性"></a>3.5 grid-auto-flow 属性</h3><p>划分网格以后，容器的子元素会按照顺序，自动放置在每一个网格。默认的放置顺序是”先行后列”，即先填满第一行，再开始放入第二行，即下图数字的顺序。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109115239.png" alt="2020240109115239.png"> </p>
<p>这个顺序由<code>grid-auto-flow</code>属性决定，默认值是<code>row</code>，即”先行后列”。也可以将它设成<code>column</code>，变成”先列后行”。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="attribute">grid-auto-flow</span>: column;</span><br></pre></td></tr></table></figure>

<p>上面代码设置了<code>column</code>以后，放置顺序就变成了下图。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109115231.png" alt="2020240109115231.png"> </p>
<p><code>grid-auto-flow</code>属性除了设置成<code>row</code>和<code>column</code>，还可以设成<code>row dense</code>和<code>column dense</code>。这两个值主要用于，某些项目指定位置以后，剩下的项目怎么自动放置。</p>
<p>下面的例子让1号项目和2号项目各占据两个单元格，然后在默认的<code>grid-auto-flow: row</code>情况下，会产生下面这样的布局。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109115257.png" alt="2020240109115257.png"> </p>
<p>上图中，1号项目后面的位置是空的，这是因为3号项目默认跟着2号项目，所以会排在2号项目后面。</p>
<p>现在修改设置，设为<code>row dense</code>，表示”先行后列”，并且尽可能紧密填满，尽量不出现空格。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="attribute">grid-auto-flow</span>: row dense;</span><br></pre></td></tr></table></figure>

<p>上面的效果如下。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109115347.png" alt="2020240109115347.png"></p>
<p>上图会先填满第一行，再填满第二行，所以3号项目就会紧跟在1号项目的后面。8号项目和9号项目就会排到第四行。</p>
<p>如果将设置改为<code>column dense</code>，表示”先列后行”，并且尽量填满空格。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="attribute">grid-auto-flow</span>: column dense;</span><br></pre></td></tr></table></figure>

<p>上面的代码效果如下。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109140955.png" alt="2020240109140955.png"> </p>
<p>上图会先填满第一列，再填满第2列，所以3号项目在第一列，4号项目在第二列。8号项目和9号项目被挤到了第四列。</p>
<h3 id="3-6-justify-items-属性、align-items-属性、place-items-属性"><a href="#3-6-justify-items-属性、align-items-属性、place-items-属性" class="headerlink" title="3.6 justify-items 属性、align-items 属性、place-items 属性"></a>3.6 justify-items 属性、align-items 属性、place-items 属性</h3><p><code>justify-items</code>属性设置单元格内容的水平位置（左中右），<code>align-items</code>属性设置单元格内容的垂直位置（上中下）。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  justify-items: start | end | center | stretch;</span><br><span class="line">  <span class="attribute">align-items</span>: start | end | center | stretch;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这两个属性的写法完全相同，都可以取下面这些值。</p>
<ul>
<li>start：对齐单元格的起始边缘。</li>
<li>end：对齐单元格的结束边缘。</li>
<li>center：单元格内部居中。</li>
<li>stretch：拉伸，占满单元格的整个宽度（默认值）。</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  justify-items: start;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码表示，单元格的内容左对齐，效果如下图。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109141314.png" alt="2020240109141314.png"> </p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  <span class="attribute">align-items</span>: start;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码表示，单元格的内容头部对齐，效果如下图。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109141340.png" alt="2020240109141340.png"> </p>
<p><code>place-items</code>属性是<code>align-items</code>属性和<code>justify-items</code>属性的合并简写形式。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">place-items: &lt;align-items&gt; &lt;justify-items&gt;;</span><br></pre></td></tr></table></figure>

<p>下面是一个例子。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">place-items: start end;</span><br></pre></td></tr></table></figure>

<p>如果省略第二个值，则浏览器认为与第一个值相等。</p>
<h3 id="3-7-justify-content-属性、align-content-属性、place-content-属性"><a href="#3-7-justify-content-属性、align-content-属性、place-content-属性" class="headerlink" title="3.7  justify-content 属性、align-content 属性、place-content 属性"></a>3.7  justify-content 属性、align-content 属性、place-content 属性</h3><p><code>justify-content</code>属性是整个内容区域在容器里面的水平位置（左中右），<code>align-content</code>属性是整个内容区域的垂直位置（上中下）。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  <span class="attribute">justify-content</span>: start | end | center | stretch | space-around | space-between | space-evenly;</span><br><span class="line">  <span class="attribute">align-content</span>: start | end | center | stretch | space-around | space-between | space-evenly;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这两个属性的写法完全相同，都可以取下面这些值。（下面的图都以<code>justify-content</code>属性为例，<code>align-content</code>属性的图完全一样，只是将水平方向改成垂直方向。）</p>
<ul>
<li>start - 对齐容器的起始边框。</li>
</ul>
<p><img src="/sequoia/images/Pasted%20image%2020240109141627.png" alt="2020240109141627.png"></p>
<ul>
<li>end - 对齐容器的结束边框。</li>
</ul>
<p><img src="/sequoia/images/Pasted%20image%2020240109141805.png" alt="2020240109141805.png"></p>
<ul>
<li>center - 容器内部居中。</li>
</ul>
<p><img src="/sequoia/images/Pasted%20image%2020240109141821.png" alt="2020240109141821.png"></p>
<ul>
<li>stretch - 项目大小没有指定时，拉伸占据整个网格容器。</li>
</ul>
<p><img src="/sequoia/images/Pasted%20image%2020240109141835.png" alt="2020240109141835.png"></p>
<ul>
<li>space-around - 每个项目两侧的间隔相等。所以，项目之间的间隔比项目与容器边框的间隔大一倍。</li>
</ul>
<p><img src="/sequoia/images/Pasted%20image%2020240109141847.png" alt="2020240109141847.png"></p>
<ul>
<li>space-between - 项目与项目的间隔相等，项目与容器边框之间没有间隔。</li>
</ul>
<p><img src="/sequoia/images/Pasted%20image%2020240109141859.png" alt="2020240109141859.png"></p>
<ul>
<li>space-evenly - 项目与项目的间隔相等，项目与容器边框之间也是同样长度的间隔。</li>
</ul>
<p><img src="/sequoia/images/Pasted%20image%2020240109141915.png" alt="2020240109141915.png"></p>
<p><code>place-content</code>属性是<code>align-content</code>属性和<code>justify-content</code>属性的合并简写形式。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">place-<span class="attribute">content</span>: &lt;align-content&gt; &lt;justify-content&gt;</span><br></pre></td></tr></table></figure>

<p>下面是一个例子。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">place-<span class="attribute">content</span>: space-around space-evenly;</span><br></pre></td></tr></table></figure>

<p>如果省略第二个值，浏览器就会假定第二个值等于第一个值。</p>
<h3 id="3-8-grid-auto-columns-属性、grid-auto-rows-属性"><a href="#3-8-grid-auto-columns-属性、grid-auto-rows-属性" class="headerlink" title="3.8  grid-auto-columns 属性、grid-auto-rows 属性"></a>3.8  grid-auto-columns 属性、grid-auto-rows 属性</h3><p>有时候，一些项目的指定位置，在现有网格的外部。比如网格只有3列，但是某一个项目指定在第5行。这时，浏览器会自动生成多余的网格，以便放置项目。</p>
<p><code>grid-auto-columns</code>属性和<code>grid-auto-rows</code>属性用来设置，浏览器自动创建的多余网格的列宽和行高。它们的写法与<code>grid-template-columns</code>和<code>grid-template-rows</code>完全相同。如果不指定这两个属性，浏览器完全根据单元格内容的大小，决定新增网格的列宽和行高。</p>
<p>下面的例子里面，划分好的网格是3行 x 3列，但是，8号项目指定在第4行，9号项目指定在第5行。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  <span class="attribute">display</span>: grid;</span><br><span class="line">  <span class="attribute">grid-template-columns</span>: <span class="number">100px</span> <span class="number">100px</span> <span class="number">100px</span>;</span><br><span class="line">  <span class="attribute">grid-template-rows</span>: <span class="number">100px</span> <span class="number">100px</span> <span class="number">100px</span>;</span><br><span class="line">  <span class="attribute">grid-auto-rows</span>: <span class="number">50px</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码指定新增的行高统一为50px（原始的行高为100px）。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109142044.png" alt="2020240109142044.png"></p>
<h3 id="3-9-grid-template-属性、grid-属性"><a href="#3-9-grid-template-属性、grid-属性" class="headerlink" title="3.9  grid-template 属性、grid 属性"></a>3.9  grid-template 属性、grid 属性</h3><p><code>grid-template</code>属性是<code>grid-template-columns</code>、<code>grid-template-rows</code>和<code>grid-template-areas</code>这三个属性的合并简写形式。</p>
<p><code>grid</code>属性是<code>grid-template-rows</code>、<code>grid-template-columns</code>、<code>grid-template-areas</code>、 <code>grid-auto-rows</code>、<code>grid-auto-columns</code>、<code>grid-auto-flow</code>这六个属性的合并简写形式。</p>
<p>从易读易写的角度考虑，还是建议不要合并属性，所以这里就不详细介绍这两个属性了。</p>
<h2 id="项目属性"><a href="#项目属性" class="headerlink" title="项目属性"></a>项目属性</h2><hr>
<h3 id="4-1"><a href="#4-1" class="headerlink" title="4.1"></a>4.1</h3><p>grid-column-start 属性，<br>grid-column-end 属性，<br>grid-row-start 属性，<br>grid-row-end 属性</p>
<p>项目的位置是可以指定的，具体方法就是指定项目的四个边框，分别定位在哪根网格线。</p>
<ul>
<li><code>grid-column-start</code>属性：左边框所在的垂直网格线</li>
<li><code>grid-column-end</code>属性：右边框所在的垂直网格线</li>
<li><code>grid-row-start</code>属性：上边框所在的水平网格线</li>
<li><code>grid-row-end</code>属性：下边框所在的水平网格线</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item-1</span> &#123;</span><br><span class="line">  <span class="attribute">grid-column-start</span>: <span class="number">2</span>;</span><br><span class="line">  <span class="attribute">grid-column-end</span>: <span class="number">4</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码指定，1号项目的左边框是第二根垂直网格线，右边框是第四根垂直网格线。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109142208.png" alt="2020240109142208.png"></p>
<p>上图中，只指定了1号项目的左右边框，没有指定上下边框，所以会采用默认位置，即上边框是第一根水平网格线，下边框是第二根水平网格线。</p>
<p>除了1号项目以外，其他项目都没有指定位置，由浏览器自动布局，这时它们的位置由容器的<code>grid-auto-flow</code>属性决定，这个属性的默认值是<code>row</code>，因此会”先行后列”进行排列。读者可以把这个属性的值分别改成<code>column</code>、<code>row dense</code>和<code>column dense</code>，看看其他项目的位置发生了怎样的变化。</p>
<p>下面的例子是指定四个边框位置的效果。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item-1</span> &#123;</span><br><span class="line">  <span class="attribute">grid-column-start</span>: <span class="number">1</span>;</span><br><span class="line">  <span class="attribute">grid-column-end</span>: <span class="number">3</span>;</span><br><span class="line">  <span class="attribute">grid-row-start</span>: <span class="number">2</span>;</span><br><span class="line">  <span class="attribute">grid-row-end</span>: <span class="number">4</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/sequoia/images/Pasted%20image%2020240109142238.png" alt="2020240109142238.png"></p>
<p>这四个属性的值，除了指定为第几个网格线，还可以指定为网格线的名字。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item-1</span> &#123;</span><br><span class="line">  <span class="attribute">grid-column-start</span>: header-start;</span><br><span class="line">  <span class="attribute">grid-column-end</span>: header-end;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码中，左边框和右边框的位置，都指定为网格线的名字。</p>
<p>这四个属性的值还可以使用<code>span</code>关键字，表示”跨越”，即左右边框（上下边框）之间跨越多少个网格。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item-1</span> &#123;</span><br><span class="line">  <span class="attribute">grid-column-start</span>: span <span class="number">2</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的代码表示，1号项目的左边框距离右边框跨越2个网格。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109142318.png" alt="2020240109142318.png"></p>
<p>这与下面的代码效果完全一样。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item-1</span> &#123;</span><br><span class="line">  <span class="attribute">grid-column-end</span>: span <span class="number">2</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用这四个属性，如果产生了项目的重叠，则使用<code>z-index</code>属性指定项目的重叠顺序。</p>
<h3 id="4-2-grid-column-属性、grid-row-属性"><a href="#4-2-grid-column-属性、grid-row-属性" class="headerlink" title="4.2  grid-column 属性、grid-row 属性"></a>4.2  grid-column 属性、grid-row 属性</h3><p><code>grid-column</code>属性是<code>grid-column-start</code>和<code>grid-column-end</code>的合并简写形式，<code>grid-row</code>属性是<code>grid-row-start</code>属性和<code>grid-row-end</code>的合并简写形式。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item</span> &#123;</span><br><span class="line">  <span class="attribute">grid-column</span>: &lt;start-line&gt; / &lt;end-line&gt;;</span><br><span class="line">  <span class="attribute">grid-row</span>: &lt;start-line&gt; / &lt;end-line&gt;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面是一个例子。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item-1</span> &#123;</span><br><span class="line">  <span class="attribute">grid-column</span>: <span class="number">1</span> / <span class="number">3</span>;</span><br><span class="line">  <span class="attribute">grid-row</span>: <span class="number">1</span> / <span class="number">2</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 等同于 */</span></span><br><span class="line"><span class="selector-class">.item-1</span> &#123;</span><br><span class="line">  <span class="attribute">grid-column-start</span>: <span class="number">1</span>;</span><br><span class="line">  <span class="attribute">grid-column-end</span>: <span class="number">3</span>;</span><br><span class="line">  <span class="attribute">grid-row-start</span>: <span class="number">1</span>;</span><br><span class="line">  <span class="attribute">grid-row-end</span>: <span class="number">2</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码中，项目<code>item-1</code>占据第一行，从第一根列线到第三根列线。</p>
<p>这两个属性之中，也可以使用<code>span</code>关键字，表示跨越多少个网格。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item-1</span> &#123;</span><br><span class="line">  <span class="attribute">background</span>: <span class="number">#b03532</span>;</span><br><span class="line">  <span class="attribute">grid-column</span>: <span class="number">1</span> / <span class="number">3</span>;</span><br><span class="line">  <span class="attribute">grid-row</span>: <span class="number">1</span> / <span class="number">3</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 等同于 */</span></span><br><span class="line"><span class="selector-class">.item-1</span> &#123;</span><br><span class="line">  <span class="attribute">background</span>: <span class="number">#b03532</span>;</span><br><span class="line">  <span class="attribute">grid-column</span>: <span class="number">1</span> / span <span class="number">2</span>;</span><br><span class="line">  <span class="attribute">grid-row</span>: <span class="number">1</span> / span <span class="number">2</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码中，项目<code>item-1</code>占据的区域，包括第一行 + 第二行、第一列 + 第二列。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109142456.png" alt="2020240109142456.png"></p>
<p>斜杠以及后面的部分可以省略，默认跨越一个网格。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item-1</span> &#123;</span><br><span class="line">  <span class="attribute">grid-column</span>: <span class="number">1</span>;</span><br><span class="line">  <span class="attribute">grid-row</span>: <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码中，项目<code>item-1</code>占据左上角第一个网格。</p>
<h3 id="4-3-grid-area-属性"><a href="#4-3-grid-area-属性" class="headerlink" title="4.3 grid-area 属性"></a>4.3 grid-area 属性</h3><p><code>grid-area</code>属性指定项目放在哪一个区域。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item-1</span> &#123;</span><br><span class="line">  <span class="attribute">grid-area</span>: e;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码中，1号项目位于<code>e</code>区域，效果如下图。</p>
<p><img src="/sequoia/images/Pasted%20image%2020240109142644.png" alt="2020240109142644.png"></p>
<p><code>grid-area</code>属性还可用作<code>grid-row-start</code>、<code>grid-column-start</code>、<code>grid-row-end</code>、<code>grid-column-end</code>的合并简写形式，直接指定项目的位置。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item</span> &#123;</span><br><span class="line">  <span class="attribute">grid-area</span>: &lt;row-start&gt; / &lt;column-start&gt; / &lt;row-end&gt; / &lt;column-end&gt;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面是一个例子。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item-1</span> &#123;</span><br><span class="line">  <span class="attribute">grid-area</span>: <span class="number">1</span> / <span class="number">1</span> / <span class="number">3</span> / <span class="number">3</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-4-justify-self-属性、align-self-属性、place-self-属性"><a href="#4-4-justify-self-属性、align-self-属性、place-self-属性" class="headerlink" title="4.4  justify-self 属性、align-self 属性、place-self 属性"></a>4.4  justify-self 属性、align-self 属性、place-self 属性</h3><p><code>justify-self</code>属性设置单元格内容的水平位置（左中右），跟<code>justify-items</code>属性的用法完全一致，但只作用于单个项目。</p>
<p><code>align-self</code>属性设置单元格内容的垂直位置（上中下），跟<code>align-items</code>属性的用法完全一致，也是只作用于单个项目。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item</span> &#123;</span><br><span class="line">  justify-self: start | end | center | stretch;</span><br><span class="line">  <span class="attribute">align-self</span>: start | end | center | stretch;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这两个属性都可以取下面四个值。</p>
<ul>
<li>start：对齐单元格的起始边缘。</li>
<li>end：对齐单元格的结束边缘。</li>
<li>center：单元格内部居中。</li>
<li>stretch：拉伸，占满单元格的整个宽度（默认值）。</li>
</ul>
<p>下面是<code>justify-self: start</code>的例子。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item-1</span>  &#123;</span><br><span class="line">  justify-self: start;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/sequoia/images/Pasted%20image%2020240109142821.png" alt="2020240109142821.png"></p>
<p><code>place-self</code>属性是<code>align-self</code>属性和<code>justify-self</code>属性的合并简写形式。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">place-self: &lt;align-self&gt; &lt;justify-self&gt;;</span><br></pre></td></tr></table></figure>

<p>下面是一个例子。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">place-self: center center;</span><br></pre></td></tr></table></figure>

<p>如果省略第二个值，<code>place-self</code>属性会认为这两个值相等。</p>
<h2 id="转载链接"><a href="#转载链接" class="headerlink" title="转载链接"></a>转载链接</h2><hr>
<p><a href="https://www.ruanyifeng.com/blog/2019/03/grid-layout-tutorial.html">阮一峰 - CSS Grid 网格布局教程</a></p>
]]></content>
      <categories>
        <category>前端技术</category>
      </categories>
      <tags>
        <tag>CSS3</tag>
      </tags>
  </entry>
  <entry>
    <title>函数式编程实例之node.js图片处理工具sharp使用指南</title>
    <url>/sequoia/posts/fe0d3e08.html</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>上一篇文章简单介绍了一下<strong>函数式编程</strong>，碰巧最近基于 <code>Node.js</code> 和 <code>sharp</code> 模块做了一个图片压缩工具，想写一篇文章记录一下。</p>
<p>于是就用函数式编程思想对这个工具就行了重写，本文会着重介绍 <code>sharp</code> 模块的应用，所以和我自己做的工具有关系但是不大，为了方便理解函数式编程我拆解掉了一些复杂的业务功能。</p>
<p>本文旨在介绍 <code>sharp</code> 模块使用的同时，向大家展示函数式编程的应用，因笔者能力有限，文中难免有错误指出，欢迎大家评论区留言勘误。</p>
<h2 id="sharp-介绍"><a href="#sharp-介绍" class="headerlink" title="sharp 介绍"></a>sharp 介绍</h2><p>sharp.js 是一个流行的 Node.js 图像处理库，它提供了快速、高质量的图像处理功能。sharp.js 可以用于调整图像大小、裁剪、旋转、锐化、模糊、添加水印等各种图像处理操作。它支持多种图像格式，并提供了灵活的选项和链式调用的方式来进行图像处理。</p>
<p>sharp 是基于 libvips 库实现的。libvips 是一个开源的图像处理库，它具有高性能和低内存消耗的特点。sharp 利用 libvips 提供的强大功能和优化算法来实现快速且高质量的图像处理操作。</p>
<p>sharp 利用 libvips 提供的功能，提供了一种简单而高效的 JavaScript 接口，使开发者能够在 Node.js 环境中轻松进行图像处理。通过与 libvips 的紧密集成，sharp 可以充分利用 libvips 的性能和优化，从而实现快速的图像处理操作。</p>
<p>因此，sharp 的强大和高性能主要来自于底层的 libvips 库，而 sharp 则提供了一个友好的 JavaScript 接口，使开发者能够便捷地使用 libvips 进行图像处理。</p>
<p><a href="https://sharp.pixelplumbing.com/">sharp官网：https://sharp.pixelplumbing.com/</a></p>
<p><a href="https://github.com/lovell/sharp">Github项目：https://github.com/lovell/sharp</a></p>
<h3 id="sharp-使用"><a href="#sharp-使用" class="headerlink" title="sharp 使用"></a>sharp 使用</h3><p>sharp 使用非常简单，它支持像 <code>jQuery</code> 那样的链式调用。它的使用非常简单，下面是一个简单实例，这个实例把一张图片大小调整成300*300，然后进行了裁切，并且把图片质量调整到10来减少图片size，最后保存到本地。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 引入</span></span><br><span class="line"><span class="keyword">const</span> sharp = <span class="built_in">require</span>(<span class="string">&#x27;sharp&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用</span></span><br><span class="line"></span><br><span class="line"><span class="title function_">sharp</span>(<span class="string">&#x27;./images/测试图片.jpg&#x27;</span>)</span><br><span class="line">    .<span class="title function_">resize</span>(<span class="number">300</span>, <span class="number">300</span>)</span><br><span class="line">    .<span class="title function_">extract</span>(&#123;<span class="attr">left</span>: <span class="number">100</span>, <span class="attr">top</span>: <span class="number">100</span>, <span class="attr">width</span>: <span class="number">200</span>, <span class="attr">height</span>: <span class="number">200</span>&#125;)</span><br><span class="line">    .<span class="title function_">jpeg</span>(&#123; <span class="attr">quality</span>: <span class="number">10</span> &#125;)</span><br><span class="line">    .<span class="title function_">toFile</span>(<span class="string">&#x27;./output/demo.jpg&#x27;</span>);</span><br></pre></td></tr></table></figure>

<p>使用起来很简单，接下来，我们来用函数式编程的思想对它进行重写。</p>
<h2 id="实例讲解"><a href="#实例讲解" class="headerlink" title="实例讲解"></a>实例讲解</h2><p>这个实例实现了对图片的缩放、裁剪、格式转换、定制图片优化等功能。使用函数式编程思想把一个个小功能定义成对应的纯函数。最后在处理图片的时候进行组合，实现最终的功能。为了方便功能的最后组合，封装了一个 <code>pipe</code> 管道函数，让所有的方法如同在流水线上一下，按一定的顺序进行执行。</p>
<p>单独的功能函数都不复杂，如果阅读工程中无法串起来，可以直接看下面的调用。</p>
<h3 id="压缩图片"><a href="#压缩图片" class="headerlink" title="压缩图片"></a>压缩图片</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> 缩放、压缩图片</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">Number</span>&#125; width 图片宽度</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">Number</span>&#125; height 图片高度</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">resizeImage</span>(<span class="params">width, height</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">async</span> <span class="keyword">function</span> (<span class="params">imageBuffer</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">await</span> <span class="title function_">sharp</span>(<span class="keyword">await</span> imageBuffer).<span class="title function_">resize</span>(width, height).<span class="title function_">toBuffer</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="裁剪图片"><a href="#裁剪图片" class="headerlink" title="裁剪图片"></a>裁剪图片</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> 提取、裁切图片</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">Number</span>&#125; x x轴</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">Number</span>&#125; y y轴</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">Number</span>&#125; w 截取宽度</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">number</span>&#125; h 截取高度</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">cropImage</span>(<span class="params">x, y, w, h</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">async</span> <span class="keyword">function</span> (<span class="params">imageBuffer</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="title function_">sharp</span>(<span class="keyword">await</span> imageBuffer).<span class="title function_">extract</span>(&#123; <span class="attr">left</span>: x, <span class="attr">top</span>: y, <span class="attr">width</span>: w, <span class="attr">height</span>: h&#125;).<span class="title function_">toBuffer</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="JPG图片处理"><a href="#JPG图片处理" class="headerlink" title="JPG图片处理"></a>JPG图片处理</h3><p>JPG图片处理的可选参数如下：</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>类型</th>
<th>默认值</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td>quality</td>
<td>number</td>
<td>80</td>
<td>质量，整数 1-100</td>
</tr>
<tr>
<td>progressive</td>
<td>boolean</td>
<td>false</td>
<td>使用逐行（隔行）扫描</td>
</tr>
<tr>
<td>chromaSubsampling</td>
<td>string</td>
<td>“‘4:2:0’”</td>
<td>设置为“4:4:4”以防止色度子采样，否则默认为“4:2:0”色度子采样</td>
</tr>
<tr>
<td>optimiseCoding</td>
<td>boolean</td>
<td>true</td>
<td>优化哈夫曼编码表</td>
</tr>
<tr>
<td>optimizeCoding</td>
<td>boolean</td>
<td>true</td>
<td>optimiseCoding 的替代拼写</td>
</tr>
<tr>
<td>mozjpeg</td>
<td>boolean</td>
<td>false</td>
<td>使用 mozjpeg 默认值，相当于{ trellisQuantisation: true, overshootDeringing: true, optimiseScans: true, quantisationTable: 3 }</td>
</tr>
<tr>
<td>trellisQuantization</td>
<td>boolean</td>
<td>false</td>
<td>应用网格量化</td>
</tr>
<tr>
<td>overshootDeringing</td>
<td>boolean</td>
<td>false</td>
<td>应用过冲去振铃</td>
</tr>
<tr>
<td>optimiseScans</td>
<td>boolean</td>
<td>false</td>
<td>优化逐行扫描，强制逐行扫描</td>
</tr>
<tr>
<td>optimizeScans</td>
<td>boolean</td>
<td>false</td>
<td>optimiseScans 的替代拼写</td>
</tr>
<tr>
<td>quantisationTable</td>
<td>number</td>
<td>0</td>
<td>要使用的量化表，整数 0-8</td>
</tr>
<tr>
<td>quantizationTable</td>
<td>number</td>
<td>0</td>
<td>quantizationTable 的替代拼写</td>
</tr>
<tr>
<td>force</td>
<td>boolean</td>
<td>true</td>
<td>强制 JPEG 输出，否则尝试使用输入格式</td>
</tr>
</tbody></table>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> 对jpg图片进行处理</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">Object</span>&#125; options </span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">convertToJPG</span>(<span class="params">options</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">async</span> <span class="keyword">function</span>(<span class="params">imageBuffer</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="title function_">sharp</span>(<span class="keyword">await</span> imageBuffer).<span class="title function_">jpeg</span>(options).<span class="title function_">toBuffer</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="PNG图片处理"><a href="#PNG图片处理" class="headerlink" title="PNG图片处理"></a>PNG图片处理</h3><p>PNG图片处理的可选参数如下：</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>类型</th>
<th>默认值</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td>progressive</td>
<td>boolean</td>
<td>false</td>
<td>使用逐行（隔行）扫描</td>
</tr>
<tr>
<td>compressionLevel</td>
<td>number</td>
<td>6</td>
<td>zlib 压缩级别，0（最快、最大）到 9（最慢、最小）</td>
</tr>
<tr>
<td>adaptiveFiltering</td>
<td>boolean</td>
<td>false</td>
<td>使用自适应行过滤</td>
</tr>
<tr>
<td>palette</td>
<td>boolean</td>
<td>false</td>
<td>量化为具有 alpha 透明度支持的基于调色板的图像</td>
</tr>
<tr>
<td>quality</td>
<td>number</td>
<td>100</td>
<td>使用达到给定质量所需的最少颜色数，设置palette为true</td>
</tr>
<tr>
<td>effort</td>
<td>number</td>
<td>7</td>
<td>CPU 工作量介于 1（最快）和 10（最慢）之间，设置palette为true</td>
</tr>
<tr>
<td>colours</td>
<td>number</td>
<td>256</td>
<td>调色板条目的最大数量，设置palette为true</td>
</tr>
<tr>
<td>colors</td>
<td>number</td>
<td>256</td>
<td>options.colours,的替代拼写palette为true</td>
</tr>
<tr>
<td>dither</td>
<td>number</td>
<td>1.0</td>
<td>Floyd-Steinberg 误差扩散水平，设置palette为true</td>
</tr>
<tr>
<td>force</td>
<td>boolean</td>
<td>true</td>
<td>强制 PNG 输出，否则尝试使用输入格式</td>
</tr>
</tbody></table>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> 对png图片进行处理</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">Object</span>&#125; options </span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">convertToPNG</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">async</span> <span class="keyword">function</span>(<span class="params">imageBuffer</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="title function_">sharp</span>(<span class="keyword">await</span> imageBuffer).<span class="title function_">png</span>(options).<span class="title function_">toBuffer</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="转变图片格式"><a href="#转变图片格式" class="headerlink" title="转变图片格式"></a>转变图片格式</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> 转变图片格式</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">*</span>&#125; format </span></span><br><span class="line"><span class="comment"> * <span class="doctag">@returns</span> </span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">convertToFormat</span> (format) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">async</span> <span class="keyword">function</span> (<span class="params">imageBuffer</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="title function_">sharp</span>(<span class="keyword">await</span> imageBuffer).<span class="title function_">toFormat</span>(format).<span class="title function_">toBuffer</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="保存图片"><a href="#保存图片" class="headerlink" title="保存图片"></a>保存图片</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> 保存图片到指定路径</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">String</span>&#125; 图片保存路径</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">saveImage</span>(<span class="params">outputPath</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">async</span> <span class="keyword">function</span> (<span class="params">imageBuffer</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="title function_">sharp</span>(<span class="keyword">await</span> imageBuffer).<span class="title function_">toFile</span>(outputPath)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="管道-辅助-函数"><a href="#管道-辅助-函数" class="headerlink" title="管道(辅助)函数"></a>管道(辅助)函数</h3><p>管道函数是一个编程概念，用于将多个函数连接在一起，形成一个函数链。每个函数的输出作为下一个函数的输入，数据在这个函数链中顺序流动，类似于管道中的流体。</p>
<p>管道函数的主要作用是简化代码和增强可读性。通过将多个函数链接在一起，可以将复杂的操作拆分成更小、更可管理的部分。每个函数只需关注自己的输入和输出，而不需要关心整个处理流程。这样可以降低代码的复杂性和维护成本。</p>
<p>另外，管道函数还可以提高代码的可组合性和可重用性。每个函数都可以在不同的上下文中使用，只需根据需要重新组合函数链即可。这种组合性使得代码更加灵活，可以根据需求进行定制化的数据处理。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> 辅助函数</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">Function</span>&#125; 管道函数</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">pipe</span>(<span class="params">...fns</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">function</span> (<span class="params">x</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> fns.<span class="title function_">reduce</span>(<span class="function">(<span class="params">v, fn</span>) =&gt;</span> <span class="title function_">fn</span>(v), x);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="功能组合"><a href="#功能组合" class="headerlink" title="功能组合"></a>功能组合</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> 组合功能实现对图片的处理</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">*</span>&#125; inputPath 图片输入路径</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">*</span>&#125; outputPath 图片输出路径</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">*</span>&#125; options 其他图片配置</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@returns</span> </span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">processImage</span> (inputPath, outputPath, options) &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123; width, height, format, crop &#125; = options;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 压缩和裁剪图片</span></span><br><span class="line">    <span class="keyword">const</span> resizeAndCrop = <span class="title function_">pipe</span>(</span><br><span class="line">        <span class="title function_">resizeImage</span>(width, height),</span><br><span class="line">        crop ? <span class="title function_">cropImage</span>(crop.<span class="property">x</span>, crop.<span class="property">y</span>, crop.<span class="property">w</span>, crop.<span class="property">h</span>) : <span class="function">(<span class="params">imageBuffer</span>) =&gt;</span> imageBuffer</span><br><span class="line">    )</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 保存图片</span></span><br><span class="line">    <span class="keyword">const</span> saveToFile = <span class="title function_">saveImage</span>(outputPath);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 组合调用</span></span><br><span class="line">    <span class="keyword">return</span> <span class="title function_">pipe</span>(</span><br><span class="line">        <span class="function">() =&gt;</span> <span class="title function_">sharp</span>(inputPath).<span class="title function_">toBuffer</span>(),</span><br><span class="line">        resizeAndCrop,</span><br><span class="line">        <span class="title function_">convertToFormat</span>(format),</span><br><span class="line">        <span class="title function_">convertToPNG</span>(&#123; <span class="attr">quality</span>: <span class="number">10</span>&#125;),</span><br><span class="line">        saveToFile</span><br><span class="line">    )();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="实例应用"><a href="#实例应用" class="headerlink" title="实例应用"></a>实例应用</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> inputPath = <span class="string">&#x27;./images/测试图片.jpg&#x27;</span>;</span><br><span class="line"><span class="keyword">const</span> outputPath = <span class="string">&#x27;./output/demo.png&#x27;</span>;</span><br><span class="line"><span class="keyword">const</span> options = &#123;</span><br><span class="line">    <span class="attr">width</span>: <span class="number">500</span>,</span><br><span class="line">    <span class="attr">height</span>: <span class="number">400</span>,</span><br><span class="line">    <span class="attr">format</span>: <span class="string">&#x27;png&#x27;</span>,</span><br><span class="line">    <span class="attr">crop</span>: &#123;</span><br><span class="line">        <span class="attr">x</span>: <span class="number">100</span>,</span><br><span class="line">        <span class="attr">y</span>: <span class="number">100</span>,</span><br><span class="line">        <span class="attr">w</span>: <span class="number">300</span>,</span><br><span class="line">        <span class="attr">h</span>: <span class="number">300</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="title function_">processImage</span>(inputPath, outputPath, options).<span class="title function_">then</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;图片处理并保存成功&#x27;</span>);</span><br><span class="line">&#125;).<span class="title function_">catch</span>(<span class="function">(<span class="params">err</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;图片处理过程中发生错误：&#x27;</span>, err.<span class="property">message</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>本文简要介绍了 <code>sharp</code> 模块，它是一个基于 Node.js 的高性能图像处理库。通过与底层的 libvips 库紧密集成，sharp 可以实现快速且高质量的图像处理操作。同时，本文还向大家展示了函数式编程思想在实际项目中的应用。通过将复杂的业务逻辑拆解成小的纯函数，并利用管道函数进行组合，可以实现代码的简化、可读性的增强，同时还提高了代码的可组合性和可重用性。希望本文能够帮助大家更好地理解 <code>sharp</code> 模块的使用，并对函数式编程的应用有进一步的认识。</p>
]]></content>
      <categories>
        <category>前端技术</category>
      </categories>
      <tags>
        <tag>函数式编程</tag>
        <tag>图片处理</tag>
      </tags>
  </entry>
  <entry>
    <title>前端金额运算精度丢失问题及解决方案</title>
    <url>/sequoia/posts/8a919224.html</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>前端开发中难免会遇到价格和金额计算的需求，这类需求所要计算的数值大多数情况下是要求精确到小数点后到多少位。但是因为JS语言本身的缺陷，在处理浮点数的运算时会出现一些奇怪的问题，导致计算不精确。</p>
<p>本文尝试从现象入手，分析造成这一问题原因，并总结和整合一些通用的解决方案，以供大家参考。</p>
<h2 id="现象回顾"><a href="#现象回顾" class="headerlink" title="现象回顾"></a>现象回顾</h2><p>下面的是JS进行数值运算过程中常见的问题，这个问题有个专业的名称叫<strong>精度丢失</strong>。</p>
<p>在 JavaScript 中整数和浮点数都属于 Number 数据类型，所有的数字都是以 64 位浮点数形式存储，整数也是如此。所以我们在打印 1.00 这样的浮点数的结果是 1 而非 1.00 。在一些特殊的数值表示中，例如金额，这样看上去有点别扭，但是至少值是正确了。然而要命的是，当浮点数做<strong>数学运算</strong>的时候，你经常会发现一些问题，举几个例子：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 加法</span></span><br><span class="line"><span class="number">0.1</span> + <span class="number">0.2</span> = <span class="number">0.30000000000000004</span></span><br><span class="line"><span class="number">0.7</span> + <span class="number">0.1</span> = <span class="number">0.7999999999999999</span></span><br><span class="line"><span class="number">0.2</span> + <span class="number">0.4</span> = <span class="number">0.6000000000000001</span></span><br><span class="line"><span class="number">2.22</span> + <span class="number">0.1</span> = <span class="number">2.3200000000000003</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 减法</span></span><br><span class="line"><span class="number">1.5</span> - <span class="number">1.2</span> = <span class="number">0.30000000000000004</span></span><br><span class="line"><span class="number">0.3</span> - <span class="number">0.2</span> = <span class="number">0.09999999999999998</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 乘法</span></span><br><span class="line"><span class="number">19.9</span> * <span class="number">100</span> = <span class="number">1989.9999999999998</span></span><br><span class="line"><span class="number">19.9</span> * <span class="number">10</span> * <span class="number">10</span> = <span class="number">1990</span></span><br><span class="line"><span class="number">1306377.64</span> * <span class="number">100</span> = <span class="number">130637763.99999999</span></span><br><span class="line"><span class="number">1306377.64</span> * <span class="number">10</span> * <span class="number">10</span> = <span class="number">130637763.99999999</span></span><br><span class="line"><span class="number">0.7</span> * <span class="number">180</span> = <span class="number">125.99999999999999</span></span><br><span class="line"><span class="number">9.7</span> * <span class="number">100</span> = <span class="number">969.9999999999999</span></span><br><span class="line"><span class="number">39.7</span> * <span class="number">100</span> = <span class="number">3970.0000000000005</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 除法</span></span><br><span class="line"><span class="number">0.3</span> / <span class="number">0.1</span> = <span class="number">2.9999999999999996</span></span><br><span class="line"><span class="number">0.69</span> / <span class="number">10</span> = <span class="number">0.06899999999999999</span></span><br></pre></td></tr></table></figure>

<h2 id="问题分析"><a href="#问题分析" class="headerlink" title="问题分析"></a>问题分析</h2><p>JavaScript 里的数字是采用 IEEE 754 标准的 64 位双精度浮点数。</p>
<p>该规范定义了浮点数的格式，对于 64 位的浮点数在内存中的表示，最高的 1 位是符号位，接着的 11 位是指数，剩下的 52 位为有效数字，具体对应如下：</p>
<ul>
<li>第 0 位：符号位， s 表示 ，0 表示正数，1 表示负数；</li>
<li>第 1 位到第 11 位：储存指数部分， e 表示 ；</li>
<li>第 12 位到第 63 位：储存小数部分（即有效数字），f 表示，</li>
</ul>
<p><strong>符号位决定了一个数的正负，指数部分决定了数值的大小，小数部分决定了数值的精度。</strong></p>
<p>IEEE 754 规定，有效数字第一位默认总是 1，不保存在 64 位浮点数之中。也就是说，有效数字总是 1.xx…xx 的形式，其中 xx..xx 的部分保存在 64 位浮点数之中，最长可能为 52 位。</p>
<p>因此，JavaScript 提供的有效数字最长为 53 个二进制位（64 位浮点的后 52 位 + 有效数字第一位的 1）。</p>
<p>那么，JavaScript 在计算 <code>0.1 + 0.2</code> 时，到底发生了什么呢？</p>
<p>首先，十进制的 <code>0.1</code> 和 <code>0.2</code> 都会被转换成二进制，但由于浮点数用二进制表达时是无穷的，就成了下面的样子。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0.1 -&gt; 0.0001100110011001...(无限)</span><br><span class="line">0.2 -&gt; 0.0011001100110011...(无限)</span><br></pre></td></tr></table></figure>

<p>IEEE 754 标准的 64 位双精度浮点数的小数部分最多支持 53 位二进制位，所以两者相加之后得到的二进制就是下面的样子。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0.0100110011001100110011001100110011001100110011001100</span><br></pre></td></tr></table></figure>

<p>最终，因浮点数小数位的限制而截断的二进制数字，再转换为十进制，就成了 <code>0.30000000000000004</code>，所以在进行算术计算时就产生了误差。</p>
<h2 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h2><blockquote>
<p>通常情况下对计算精度要求高的业务场景都应该交给后端去计算和存储，因为后端有成熟的方案和工具来解决这种计算问题。</p>
</blockquote>
<p>这里是对网上常见的几个解决方案的汇总和整理，但是方案一和方案二都存在一定的局限性，我会在对应的方案里进行说明。通常我们前端做这类运算通常只用于表现层，所以这两个方案基本是够用的。</p>
<h3 id="方案一"><a href="#方案一" class="headerlink" title="方案一"></a>方案一</h3><p>在应对确定精度浮点数运算时，可以把金额转换成整数进行运算，最后再将结果转换成真实金额。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义金额（保留两位小数）</span></span><br><span class="line"><span class="keyword">const</span> amount1 = <span class="number">0.10</span>;</span><br><span class="line"><span class="keyword">const</span> amount2 = <span class="number">0.20</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 转整数运算，并还原成真实金额</span></span><br><span class="line"><span class="keyword">const</span> result = ((amount1 * <span class="number">100</span>) + (amount2 * <span class="number">100</span>)) / <span class="number">100</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 结果</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(result); <span class="comment">// 0.3</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 直接运算</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(amount1 + amount2); <span class="comment">// 0.30000000000000004</span></span><br></pre></td></tr></table></figure>

<p>需要注意的是上面的例子之能处理最多两位小数的运算场景，如果小数位不确定这个方法是行不通的。</p>
<h3 id="方案二"><a href="#方案二" class="headerlink" title="方案二"></a>方案二</h3><p>JavaScript 内置的 toFixed() 方法可以将数字转换成保留指定小数位的字符串。这个方法适用于简单的金额计算。但需要注意舍入误差，因为转换后是字符串，失去了浮点数的特性，最后的结果坑你存在微小的误差。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义金额</span></span><br><span class="line"><span class="keyword">const</span> amount1 = <span class="number">0.1</span>;</span><br><span class="line"><span class="keyword">const</span> amount2 = <span class="number">0.2</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 加法运算</span></span><br><span class="line"><span class="keyword">const</span> result = (amount1 + amount2).<span class="title function_">toFixed</span>(<span class="number">2</span>); <span class="comment">// 保留两位小数</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 注意这里运算的结果应该是：0.30000000000000004</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(result); <span class="comment">// 输出 &quot;0.30&quot;</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>toFixed 它是一个四舍六入五成双的诡异的方法(也叫银行家算法)。”四舍六入五成双”含义：对于位数很多的近似数，当有效位数确定后，其后面多余的数字应该舍去，只保留有效数字最末一位，这种修约（舍入）规则是“四舍六入五成双”，也即“4舍6入5凑偶”这里“四”是指≤4 时舍去，”六”是指≥6时进上，”五”指的是根据5后面的数字来定，当5后有数时，舍5入1；当5后无有效数字时，需要分两种情况来讲：5前为奇数，舍5入1；5前为偶数，舍5不进（0是偶数）。</p>
</blockquote>
<h3 id="第三方库"><a href="#第三方库" class="headerlink" title="第三方库"></a>第三方库</h3><p>现代前端发展至今，已经有很多成熟的类库来帮助我们解决此类问题，这类类库通常有很好的通用性和兼容性。</p>
<p>下面我将推荐几个人气较高的数字计算类库。</p>
<h4 id="Math-js"><a href="#Math-js" class="headerlink" title="Math.js"></a>Math.js</h4><p>Math.js 是专门为 JavaScript 和 Node.js 提供的一个广泛的数学库。它具有灵活的表达式解析器，支持符号计算，配有大量内置函数和常量，并提供集成解决方案来处理不同的数据类型像数字，大数字 (超出安全数的数字)，复数，分数，单位和矩阵，功能强大，易于使用。</p>
<p>官网：<a href="http://mathjs.org/">http://mathjs.org/</a><br>GitHub：<a href="https://github.com/josdejong/mathjs">GitHub - josdejong&#x2F;mathjs: An extensive math library for JavaScript and Node.js</a></p>
<h4 id="decimal-js"><a href="#decimal-js" class="headerlink" title="decimal.js"></a>decimal.js</h4><p>为 JavaScript 提供十进制类型的任意精度数值。</p>
<p>官网：<a href="http://mikemcl.github.io/decimal.js/">decimal.js API</a><br>GitHub：<a href="https://github.com/MikeMcl/decimal.js">GitHub - MikeMcl&#x2F;decimal.js: An arbitrary-precision Decimal type for JavaScript</a></p>
<h4 id="big-js"><a href="#big-js" class="headerlink" title="big.js"></a>big.js</h4><p>Big.js 是一个用于处理任意精度的大数运算的 JavaScript 库。它解决了 JavaScript 中处理大数运算时精度丢失的问题，提供了更高精度的计算能力。</p>
<p>Big.js 库的特点包括：</p>
<ul>
<li>任意精度：Big.js 允许您处理任意精度的数字，而不受 JavaScript 内置数字类型的限制。</li>
<li>高精度计算：Big.js 提供了精确的加法、减法、乘法、除法和取余等运算，以及比较和舍入等功能。</li>
<li>可配置的精度和舍入规则：您可以自定义 Big.js 运算的精度和舍入规则，以满足特定的需求。</li>
<li>支持链式操作：您可以使用链式调用来执行多个运算，使代码更简洁易读。</li>
<li>适用于浏览器和 Node.js：Big.js 可以在浏览器和 Node.js 环境中使用，兼容性良好。</li>
</ul>
<p>Big.js 库非常适用于需要高精度计算的场景，如金融、密码学、科学计算和大数据处理等。它允许开发人员在 JavaScript 中进行准确的数字计算，避免了精度损失带来的问题。</p>
<p>官网：<a href="http://mikemcl.github.io/big.js">big.js API</a><br>GitHub：<a href="https://github.com/MikeMcl/big.js/">GitHub - MikeMcl&#x2F;big.js: A small, fast JavaScript library for arbitrary-precision decimal arithmetic.</a></p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>本文对 Javascript 中浮点数运算出现的精度丢失问题进行了还原，分析了问题产生的原因在于二进制本身。同时给出了三个网络是比较成熟的解决方案，其中第一和第二方案基本可以满足大部分开发场景，如果不能满足就使用类库。</p>
<p>最后，建议所有对运算精度要求极高的业务场景都放到后端去运算，切记。</p>
]]></content>
      <categories>
        <category>前端技术</category>
      </categories>
      <tags>
        <tag>数值处理</tag>
      </tags>
  </entry>
  <entry>
    <title>面试官：说说你对函数式编程的理解</title>
    <url>/sequoia/posts/802d3aed.html</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>面试中遇到一部分人对函数式编程不了解，还有一部分认为函数式编程除了面试中能用到，在自己的项目经验中很难看到具体的应用场景。</p>
<p>函数式编程作为一种编程范式由来已久。函数式编程强调不可变性和纯函数，即函数的输出仅由输入决定，没有副作用和对外部状态的依赖。这种特性使得函数式代码更易于理解、测试和调试。</p>
<p>函数式编程强调使用<strong>多个函数</strong>来<strong>组合</strong>和来处理数据。其核心是将运算过程抽象成函数，好处是可以<strong>复用</strong>。</p>
<p>本文将尝试用通俗易懂的方式讲解函数式编程在前端项目中的应用。会对函数式编程进行简单的“扫盲”，然后结合几个实例方便大家理解。</p>
<h2 id="认识函数式编程"><a href="#认识函数式编程" class="headerlink" title="认识函数式编程"></a>认识函数式编程</h2><p>在开始之前，请先看一个函数式编程的实例。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 纯函数，只做一件事，输入输出，没有副作用</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">double</span>(<span class="params">number</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> number * <span class="number">2</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">square</span>(<span class="params">number</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> number * number;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 不改变原来的数据，创建新的数组</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">doubleArray</span>(<span class="params">arr</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> arr.<span class="title function_">map</span>(double);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">squareArray</span>(<span class="params">arr</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> arr.<span class="title function_">map</span>(square);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 把多个函数组合成一个新的函数</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">compose</span>(<span class="params">...fns</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">function</span> (<span class="params">x</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> fns.<span class="title function_">reduceRight</span>(<span class="keyword">function</span>(<span class="params">acc, fn</span>)&#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="title function_">fn</span>(acc);</span><br><span class="line">    &#125;, x);</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> numbers = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>];</span><br><span class="line"><span class="keyword">const</span> processArray = <span class="title function_">compose</span>(squareArray, doubleArray);</span><br><span class="line"><span class="keyword">const</span> result = <span class="title function_">processArray</span>(numbers);</span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(result); <span class="comment">// [ 4, 16, 36, 64, 100 ]</span></span><br></pre></td></tr></table></figure>

<p>有没有发现，函数式编程在实践中就像用乐高堆积木。</p>
<p>在这个例子中，先定义了两个纯函数。遵循不可变性原则对需要运算的数组进行新建而不是改变原有数据。最后把多个功能函数组合成一个新的函数，实现需要的功能。</p>
<h3 id="纯函数"><a href="#纯函数" class="headerlink" title="纯函数"></a>纯函数</h3><p>上面的实例中定义了两个纯函数 <code>double</code> 和 <code>square</code>。那么什么样的函数是纯函数呢？</p>
<p>根据定义，如果一个函数符合两个条件，它就可以被称为纯函数：</p>
<ol>
<li>此函数在相同的输入值时，总是产生相同的输出。</li>
<li>次函数不会对除了函数返回值之外的任何东西产生可观察的副作用。不会修改传入的参数，也不会对全局状态进行更改，不会进行 I&#x2F;O 操作，也不会触发网络请求等</li>
</ol>
<p>由此可见，<strong>纯函数的执行仅仅是为了计算并返回一个值</strong>。</p>
<p>下面是一个纯函数和非纯函数的对比实例。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 纯函数，不依赖于外部状态，相同的输入总是产生相同的输出</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">add</span>(<span class="params">a, b</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">add</span>(<span class="number">2</span>, <span class="number">3</span>)); <span class="comment">// 输出 5</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">add</span>(<span class="number">2</span>, <span class="number">3</span>)); <span class="comment">// 输出 5</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 非纯函数，依赖于外部状态，相同的输入可能产生不同的输出</span></span><br><span class="line"><span class="keyword">let</span> result = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">addToResult</span>(<span class="params">value</span>) &#123;</span><br><span class="line">  result += value;</span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">addToResult</span>(<span class="number">5</span>)); <span class="comment">// 输出 5</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">addToResult</span>(<span class="number">3</span>)); <span class="comment">// 输出 8</span></span><br></pre></td></tr></table></figure>
<h3 id="函数是一等公民"><a href="#函数是一等公民" class="headerlink" title="函数是一等公民"></a>函数是一等公民</h3><p>在函数式编程中，函数被视为一等公民。函数可以像其他数据类型（如整数、字符串等）一样被操作、传递和赋值。这种特性使得函数式编程具备更高的抽象能力和灵活性，可以更方便地实现函数的组合和重用。</p>
<p><strong>1、函数可以被赋值给变量</strong></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title function_">add</span> = (<span class="params">a, b</span>) =&gt; a + b;</span><br><span class="line"><span class="keyword">const</span> <span class="title function_">multiply</span> = (<span class="params">a, b</span>) =&gt; a * b;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> operation = add; <span class="comment">// 函数赋值给变量</span></span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">operation</span>(<span class="number">2</span>, <span class="number">3</span>)); <span class="comment">// 输出: 5</span></span><br><span class="line"></span><br><span class="line">operation = multiply; <span class="comment">// 可以修改变量的值为另一个函数</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">operation</span>(<span class="number">2</span>, <span class="number">3</span>)); <span class="comment">// 输出: 6</span></span><br></pre></td></tr></table></figure>

<p><strong>2、函数可以作为参数传递</strong></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title function_">add</span> = (<span class="params">a, b</span>) =&gt; a + b;</span><br><span class="line"><span class="keyword">const</span> <span class="title function_">multiply</span> = (<span class="params">a, b</span>) =&gt; a * b;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="title function_">applyOperation</span> = (<span class="params">a, b, operation</span>) =&gt; <span class="title function_">operation</span>(a, b);</span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">applyOperation</span>(<span class="number">2</span>, <span class="number">3</span>, add)); <span class="comment">// 输出: 5</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">applyOperation</span>(<span class="number">2</span>, <span class="number">3</span>, multiply)); <span class="comment">// 输出: 6</span></span><br></pre></td></tr></table></figure>

<p><strong>3、函数可以作为返回值返回</strong></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title function_">add</span> = (<span class="params">a, b</span>) =&gt; a + b;</span><br><span class="line"><span class="keyword">const</span> <span class="title function_">multiply</span> = (<span class="params">a, b</span>) =&gt; a * b;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="title function_">createOperation</span> = (<span class="params">operator</span>) =&gt; &#123;</span><br><span class="line">  <span class="keyword">if</span> (operator === <span class="string">&#x27;+&#x27;</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> add;</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (operator === <span class="string">&#x27;*&#x27;</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> multiply;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> operation1 = <span class="title function_">createOperation</span>(<span class="string">&#x27;+&#x27;</span>);</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">operation1</span>(<span class="number">2</span>, <span class="number">3</span>)); <span class="comment">// 输出: 5</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> operation2 = <span class="title function_">createOperation</span>(<span class="string">&#x27;*&#x27;</span>);</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">operation2</span>(<span class="number">2</span>, <span class="number">3</span>)); <span class="comment">// 输出: 6</span></span><br></pre></td></tr></table></figure>

<h3 id="函数组合"><a href="#函数组合" class="headerlink" title="函数组合"></a>函数组合</h3><p>函数式编程中还有一个重要的思想就是<code>组合(compose)</code>，它允许我们把逻辑通过函数的形式进行拆分，然后进行函数之间的组合最后得到想要的完整逻辑。文章开篇的实例中已经进行了演示，这里不做重复。</p>
<p>简言之函数式编程的实践就是对业务逻辑进行拆分，变成一个个纯函数，然后在组合成一个完整的逻辑模块。</p>
<p>这样做的好处很明显，简化逻辑，复用代码，方便写单元测试。</p>
<h2 id="函数式编程库推荐"><a href="#函数式编程库推荐" class="headerlink" title="函数式编程库推荐"></a>函数式编程库推荐</h2><p>JavaScript 函数式编程的库有很多，以下是几个常用的库：</p>
<h3 id="Ramda"><a href="#Ramda" class="headerlink" title="Ramda"></a>Ramda</h3><p>Ramda 是一个专注于函数式编程的 JavaScript 库，提供了许多实用的函数和工具，用于支持函数式编程的开发风格。它强调不可变性、纯函数和柯里化，并提供了丰富的函数来处理和操作数据。</p>
<ul>
<li><a href="https://ramda.cn/">中文官网：https://ramda.cn/</a></li>
<li><a href="https://github.com/ramda/ramda">Github仓库：https://github.com/ramda/ramda</a></li>
</ul>
<h3 id="Lodash-fp"><a href="#Lodash-fp" class="headerlink" title="Lodash&#x2F;fp"></a>Lodash&#x2F;fp</h3><p>Lodash 是一个广泛使用的 JavaScript 实用工具库，而 Lodash&#x2F;fp 则是 Lodash 的函数式编程版本。它提供了许多函数式风格的函数，并采用自动柯里化和数据优先的方式。</p>
<ul>
<li><a href="https://www.lodashjs.com/">中文官网</a></li>
<li><a href="https://github.com/lodash/lodash/wiki/FP-Guide">Github仓库：https://github.com/lodash/lodash/wiki/FP-Guide</a></li>
</ul>
<h3 id="Immutable-js"><a href="#Immutable-js" class="headerlink" title="Immutable.js"></a>Immutable.js</h3><p>Immutable.js 是一个用于处理不可变数据的 JavaScript 库。它提供了一组持久化的数据结构，如 List、Map、Set 等，以及一些用于操作这些数据结构的函数。通过使用 Immutable.js，可以更轻松地管理和操作不可变数据，避免副作用和数据修改。</p>
<ul>
<li><a href="https://immutable-js.com/">官网网站：https://immutable-js.com/</a></li>
<li><a href="https://github.com/immutable-js/immutable-js/">Github仓库：https://github.com/immutable-js/immutable-js/</a></li>
</ul>
<h3 id="RxJS"><a href="#RxJS" class="headerlink" title="RxJS"></a>RxJS</h3><p>RxJS 是一个响应式编程库，它基于观察者模式和可观察序列（Observables）的概念。它提供了丰富的操作符和工具，用于处理异步数据流和事件序列。RxJS 支持函数式编程的思想，提供了一种声明式和组合的方式来处理异步操作。</p>
<ul>
<li><a href="https://cn.rx.js.org/">官方网站：https://cn.rx.js.org/</a></li>
<li><a href="https://github.com/ReactiveX/RxJS">Github：https://github.com/ReactiveX/RxJS</a></li>
</ul>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>本文简单介绍了函数式编程的基本概念和特点，包括纯函数、函数是一等公民和函数组合。旨在能够让大家方便快速的理解函数数编程是什么。函数式编程可以提高代码的可读性、可维护性和并发性，非常值得学习和应用。</p>
]]></content>
      <categories>
        <category>前端技术</category>
      </categories>
      <tags>
        <tag>函数式编程</tag>
      </tags>
  </entry>
</search>
